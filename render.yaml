services:
  - type: web
    name: sunsaft
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: sunsaft-data
      mountPath: /data
      sizeGB: 1
